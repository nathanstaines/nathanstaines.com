# Nathan Staines

My personal website that contains a minimal typing speed test built with React and Tailwind CSS.

## Getting started

```bash
npm install
npm run dev
```

## Testing

```bash
npm test
```

Tests live in `src/utils/gameUtils.test.js` and cover the three pure utility functions.
